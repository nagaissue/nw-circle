# AGENTS.md - 開発・ドキュメント作成ガイドライン

このファイルは、能開大ネットワークサークル（nw-circle）リポジトリの構成、開発プロセス、執筆ルール、および今後の活動方針を体系的にまとめた、AIエージェントおよび開発者向けの最上位インストラクションドキュメント（共通指示書）です。

---

## 1. プロジェクト概要

**nw-circle** は、能開大ネットワークサークル（Network Circle）の学習リソース、活動計画、および技術ナレッジを共有・ブラウザで閲覧可能にするための「Web資料ライブラリ＆ドキュメント管理システム」です。

- **閲覧ポータル:** [nw-circle リポジトリ閲覧ページ (Vercel)](https://nw-circle.vercel.app/)
- **主なテーマ:** 通信ネットワーク構築（Cisco Packet Tracer、実機）、OS（Linux / Windows）、クラウド（AWS）、AI（生成AI、ローカルLLM、自作Agent Skills）。
- **主要な技術スタック:**
  - **Zenn CLI:** `articles/` 下の技術記事・アイデアの執筆およびローカルプレビュー用
  - **Vercel:** 静的HTMLおよびZenn記事のホスティング
  - **Vanilla HTML / CSS:** スライド資料や講義資料、ポータルサイトのUI実装

---

## 2. ディレクトリ構成と役割

リポジトリ内の各フォルダ・ファイルは、明確な役割に基づいて整理されています。

```text
.
├── .agents/                       # エージェント用カスタムスキル (create-diary, grill-me 等)
├── .github/                       # GitHub / Copilot 用設定ファイル (copilot-instructions.md 等)
├── .gitignore
├── README.md                      # リポジトリ概要とルート階層マップ
├── AGENTS.md                      # 本ドキュメント（共通エージェント指示書）
├── GEMINI.md                      # AGENTS.md への参照リンク
├── HANDOVER.md                    # 年間計画のQ&A、未決定事項、意思決定の引き継ぎ記録
├── package.json / package-lock.json # Zenn CLI などの管理用
├── skills-lock.json
├── index.html                     # ルートディレクトリのナビゲーションハブ
│
├── articles/                      # Zenn公開用の技術記事/アイデア（Markdown形式）
├── images/                        # 記事や資料全体で使用する共通画像
│
├── docs/                          # ドキュメント・HTML資料（PDF風、レポート風の構成資料）
│   ├── index.html                 # docs/ フォルダ内のファイル一覧
│   └── network_circle_plan_2026_merged.md # 統合された年間活動計画
│
├── docs_html/                     # 講義用スライド（HTML形式）
│   ├── index.html                 # 非常に高機能な「Web資料ライブラリポータル」
│   └── assets/                    # 各スライドの画像データ（slide_*.png 形式で格納）
│
├── notes/                         # ナレッジ共有・リンク集・ブックマーク用HTML
│   └── index.html                 # リンク集ポータル
│
├── tests/                         # 新規資料やHTML/CSSの表示検証用ファイル
├── archive/                       # 過去の活動計画や古いマニュアルなどの退避先
├── trash/                         # 一時保管用ディレクトリ（原則Git追跡対象外）
└── node_modules/                  # 依存パッケージ
```

---

## 3. 主要ファイル解説

- **`index.html` (Root)**
  各サブフォルダのインデックス（`docs_html/index.html`、`docs/index.html`、`notes/index.html`）へのルーティングを行うシンプルな目次ページ。
- **`docs_html/index.html`**
  ダーク/ライトテーマ、絞り込み検索、カテゴリーフィルター（初級、中級、上級、特別講義など）を備えたリッチな「Web資料ライブラリポータル」。新しいスライドを追加した際は、このファイルのリストに手動で追加・更新を行う。
- **`HANDOVER.md`**
  サークル計画の見直し経緯、各コース（通信、クラウド、OS、AI）の目的・対象者・評価基準、未確定事項などを、Q&A形式でまとめた貴重な引き継ぎ資料。

---

## 4. 開発・コンテンツ追加ワークフロー

### A. Zenn記事 (`articles/`) の作成・編集

1. **ファイル作成:**
   `articles/` 下に `{description}_rev{num}.md` 形式（例: `cisco-cli-config_rev1.md`）でファイルを作成します。
2. **フロントマターの記述:**
   Zenn CLIが認識できるよう、すべての記事の先頭に必ず以下のYAMLフロントマターを記述してください。
   ```yaml
   ---
   title: "記事のタイトル"
   emoji: "🌐"       # 適切な絵文字
   type: "tech"     # 技術記事は "tech"、企画やアイデアは "idea"
   topics: ["network", "cisco", "plan"] # 関連キーワード
   published: true  # 公開する場合は true
   ---
   ```
3. **ローカルプレビューの実行:**
   ```bash
   npm install      # 初回のみ
   npx zenn preview # ローカルサーバー (http://localhost:8000) を起動
   ```

### B. 講義スライド・HTML資料 (`docs_html/` & `docs/`) の追加

1. **HTML/CSS 設計:**
   講義資料やスライドはレスポンシブかつ美しく構築します。既存の `docs_html/` 下のスライド群を参考に、同一のデザインシステムやアセット構成を採用してください。
2. **アセットの配置:**
   スライド内で使用する画像は `docs_html/assets/{スライド名}/slide_*.png` に格納し、HTML内からは相対パスで参照します。
3. **ポータルの更新:**
   作成したHTML資料をユーザーが探せるよう、**`docs_html/index.html`**（または `docs/index.html`）のリストカードに手動で追記します。
   - `category`、`level`（初級・中級・上級）、`tags` などのカスタム属性を適切に指定してください。

---

## 5. コーディング・執筆の標準規約

- **主言語:** 日本語 (`ja`) を使用します。高専・能開大の学生向けに、専門用語を適切に解説した丁寧で教育的な文体を心がけてください。
- **コードブロックの明記:** Cisco機器の設定やコマンド、コンフィグを記載する際は、適切なシンタックスハイライト（`bash`, `yaml`, `html`, `cisco` など）を付与してください。
- **段階的な学習設計:** サークルの活動目標として「実機を用いたVLANネットワーク構築」を置いています。初心者でもステップバイステップで理解できるよう、基礎（IPアドレッシング、ルーティング）から応用（VLAN、OS接続）へと繋がる資料設計を徹底してください。
- **セキュリティ・秘密情報の保護:**
  - Ciscoの実機認証情報、AWSのアクセスキー、ローカルNASの管理者パスワード、その他個人情報をコード内やMarkdown内に直に書き込まないこと。
  - 必要に応じてダミー値や環境変数プレースホルダーを使用してください。

---

## 6. AIエージェントとの連携・動作ルール

Gemini CLI や GitHub Copilot などのAIアシスタントが作業する際の挙動ルールです。

- **意図（Intent）の明確化:**
  ユーザーからの指示が「分析・アドバイスの要求（Inquiry）」なのか「ファイル編集の要求（Directive）」なのかを厳密に区別してください。質問に対しては解説に留め、明示的な編集指示がない限りファイルに変更を加えない。
- **サージカルなコード置換 (Surgical Edits):**
  ファイルの変更時には、対象箇所のみを正確に置換する `replace` ツールを優先し、意図しないコード消失や不要な差分の生成を回避する。
- **検証の徹底 (Validate):**
  HTMLなどの構成を変更した場合は、必ずポータルインデックス（`index.html`）との整合性を検証・確認してください。
- **コミットの取り扱い:**
  ユーザーからの明示的な指示がない限り、変更内容の `git add` や `git commit` は行わないこと。

---

*この `AGENTS.md` は、能開大ネットワークサークルの発展に伴い、活動計画が更新されるごとに適宜アップデートされます。*
